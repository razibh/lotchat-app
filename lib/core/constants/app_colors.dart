import 'package:flutter/material.dart';

class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  // Primary Colors
  static const Color primaryPurple = Color(0xFF8B5CF6);
  static const Color primaryBlue = Color(0xFF3B82F6);
  static const Color primaryGreen = Color(0xFF10B981);
  static const Color primaryOrange = Color(0xFFF59E0B);
  static const Color primaryRed = Color(0xFFEF4444);
  static const Color primaryPink = Color(0xFFEC4899);
  static const Color primaryIndigo = Color(0xFF6366F1);
  static const Color primaryTeal = Color(0xFF14B8A6);
  static const Color primaryCyan = Color(0xFF06B6D4);
  static const Color primaryYellow = Color(0xFFFBBF24);

  // Background Colors
  static const Color backgroundDark = Color(0xFF0F0F1A);
  static const Color backgroundDarker = Color(0xFF0A0A14);
  static const Color backgroundLight = Color(0xFF1A1A2E);
  static const Color backgroundLighter = Color(0xFF24243A);
  static const Color surfaceDark = Color(0xFF16213E);
  static const Color surfaceDarker = Color(0xFF0F1A2F);
  static const Color surfaceLight = Color(0xFF1F2A44);
  static const Color surfaceLighter = Color(0xFF2A3555);

  // Accent Colors
  static const Color accentPurple = Color(0xFFA78BFA);
  static const Color accentPurpleLight = Color(0xFFC4B5FD);
  static const Color accentPurpleDark = Color(0xFF7C3AED);
  static const Color accentBlue = Color(0xFF60A5FA);
  static const Color accentBlueLight = Color(0xFF93C5FD);
  static const Color accentBlueDark = Color(0xFF2563EB);
  static const Color accentGreen = Color(0xFF34D399);
  static const Color accentGreenLight = Color(0xFF6EE7B7);
  static const Color accentGreenDark = Color(0xFF059669);
  static const Color accentOrange = Color(0xFFFBBF24);
  static const Color accentOrangeLight = Color(0xFFFCD34D);
  static const Color accentOrangeDark = Color(0xFFD97706);
  static const Color accentRed = Color(0xFFF87171);
  static const Color accentRedLight = Color(0xFFFCA5A5);
  static const Color accentRedDark = Color(0xFFDC2626);
  static const Color accentPink = Color(0xFFF472B6);
  static const Color accentPinkLight = Color(0xFFF9A8D4);
  static const Color accentPinkDark = Color(0xFFDB2777);
  static const Color accentIndigo = Color(0xFF818CF8);
  static const Color accentIndigoLight = Color(0xFFA5B4FC);
  static const Color accentIndigoDark = Color(0xFF4F46E5);
  static const Color accentTeal = Color(0xFF2DD4BF);
  static const Color accentTealLight = Color(0xFF5EEAD4);
  static const Color accentTealDark = Color(0xFF0D9488);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0B0);
  static const Color textTertiary = Color(0xFF6B6B7C);
  static const Color textHint = Color(0xFF4A4A5A);
  static const Color textDisabled = Color(0xFF3A3A4A);
  static const Color textInverse = Color(0xFF000000);

  // Status Colors
  static const Color success = Color(0xFF10B981);
  static const Color successLight = Color(0xFFD1FAE5);
  static const Color successDark = Color(0xFF047857);
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color warningDark = Color(0xFFB45309);
  static const Color error = Color(0xFFEF4444);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color errorDark = Color(0xFFB91C1C);
  static const Color info = Color(0xFF3B82F6);
  static const Color infoLight = Color(0xFFDBEAFE);
  static const Color infoDark = Color(0xFF1E40AF);

  // Badge Colors
  static const Color badgeAgency = Color(0xFF9C27B0); // Purple
  static const Color badgeAgencyLight = Color(0xFFE1BEE7);
  static const Color badgeAgencyDark = Color(0xFF6A1B9A);
  static const Color badgeCoinSeller = Color(0xFFFF9800); // Orange
  static const Color badgeCoinSellerLight = Color(0xFFFFE0B2);
  static const Color badgeCoinSellerDark = Color(0xFFF57C00);
  static const Color badgeOfficial = Color(0xFF2196F3); // Blue
  static const Color badgeOfficialLight = Color(0xFFBBDEFB);
  static const Color badgeOfficialDark = Color(0xFF1976D2);
  static const Color badgeVIP = Color(0xFFF44336); // Red
  static const Color badgeVIPLight = Color(0xFFFFCDD2);
  static const Color badgeVIPDark = Color(0xFFD32F2F);
  static const Color badgeHost = Color(0xFFE91E63); // Pink
  static const Color badgeHostLight = Color(0xFFF8BBD0);
  static const Color badgeHostDark = Color(0xFFC2185B);
  static const Color badgeModerator = Color(0xFF4CAF50); // Green
  static const Color badgeModeratorLight = Color(0xFFC8E6C9);
  static const Color badgeModeratorDark = Color(0xFF388E3C);
  static const Color badgeStreamer = Color(0xFF673AB7); // Deep Purple
  static const Color badgeStreamerLight = Color(0xFFD1C4E9);
  static const Color badgeStreamerDark = Color(0xFF512DA8);

  // Tier Colors
  static const Color tierBronze = Color(0xFFCD7F32);
  static const Color tierBronzeLight = Color(0xFFE6B17E);
  static const Color tierBronzeDark = Color(0xFF8B5A2B);
  static const Color tierSilver = Color(0xFFC0C0C0);
  static const Color tierSilverLight = Color(0xFFE0E0E0);
  static const Color tierSilverDark = Color(0xFF808080);
  static const Color tierGold = Color(0xFFFFD700);
  static const Color tierGoldLight = Color(0xFFFFE55C);
  static const Color tierGoldDark = Color(0xFFB8860B);
  static const Color tierPlatinum = Color(0xFFE5E4E2);
  static const Color tierPlatinumLight = Color(0xFFF5F5F5);
  static const Color tierPlatinumDark = Color(0xFFB0B0B0);
  static const Color tierDiamond = Color(0xFFB9F2FF);
  static const Color tierDiamondLight = Color(0xFFE0F7FF);
  static const Color tierDiamondDark = Color(0xFF7FC7D9);
  static const Color tierMaster = Color(0xFFFF6B6B);
  static const Color tierMasterLight = Color(0xFFFFA8A8);
  static const Color tierMasterDark = Color(0xFFC92A2A);
  static const Color tierLegend = Color(0xFFFFD166);
  static const Color tierLegendLight = Color(0xFFFFE699);
  static const Color tierLegendDark = Color(0xFFCC9C33);

  // Game Colors
  static const Color gameChess = Color(0xFF795548);
  static const Color gameCarrom = Color(0xFFFFA726);
  static const Color gamePictionary = Color(0xFF9C27B0);
  static const Color gameTrivia = Color(0xFF2196F3);
  static const Color gameTruthOrDare = Color(0xFFE91E63);
  static const Color gameWerewolf = Color(0xFFF44336);
  static const Color gameLudo = Color(0xFF4CAF50);
  static const Color gameSnakeLadder = Color(0xFFFF9800);

  // 🟢 Fixed: 'accentYellow' is not defined, using primaryYellow instead
  static const Color _accentYellow = Color(0xFFFFE55C); // Add if needed

  // Gradient Colors
  static const Gradient gradientPrimary = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentPurple, accentBlue],
  );

  static const Gradient gradientSecondary = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentOrange, accentPink],
  );

  static const Gradient gradientSuccess = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentGreen, accentTeal],
  );

  // 🟢 Fixed: Replaced 'accentYellow' with primaryYellow
  static const Gradient gradientWarning = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentOrange, primaryYellow],
  );

  static const Gradient gradientError = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentRed, accentPink],
  );

  static const Gradient gradientInfo = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentBlue, accentIndigo],
  );

  static const Gradient gradientPurple = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [accentPurple, accentIndigo],
  );

  static const Gradient gradientGold = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [tierGold, tierGoldLight],
  );

  static const Gradient gradientSilver = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [tierSilver, tierSilverLight],
  );

  static const Gradient gradientBronze = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [tierBronze, tierBronzeLight],
  );

  static const Gradient gradientDiamond = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [tierDiamond, tierDiamondLight],
  );

  // Background Gradients
  static const Gradient gradientBackgroundDark = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [backgroundDark, backgroundDarker],
  );

  static const Gradient gradientBackgroundLight = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [backgroundLight, backgroundLighter],
  );

  static const Gradient gradientSurface = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [surfaceDark, surfaceLight],
  );

  // Glass Effect Colors
  static Color glassWhite = Colors.white.withValues(alpha: 0.1);
  static Color glassBlack = Colors.black.withValues(alpha: 0.3);
  static Color glassPurple = accentPurple.withValues(alpha: 0.1);
  static Color glassBlue = accentBlue.withValues(alpha: 0.1);
  static Color glassGreen = accentGreen.withValues(alpha: 0.1);
  static Color glassOrange = accentOrange.withValues(alpha: 0.1);
  static Color glassRed = accentRed.withValues(alpha: 0.1);

  // Border Colors
  static const Color borderLight = Color(0x1FFFFFFF);
  static const Color borderMedium = Color(0x33FFFFFF);
  static const Color borderDark = Color(0x4DFFFFFF);

  // Shadow Colors
  static Color shadowLight = Colors.black.withValues(alpha: 0.1);
  static Color shadowMedium = Colors.black.withValues(alpha: 0.2);
  static Color shadowDark = Colors.black.withValues(alpha: 0.3);
  static Color shadowPurple = accentPurple.withValues(alpha: 0.3);
  static Color shadowBlue = accentBlue.withValues(alpha: 0.3);
  static Color shadowGreen = accentGreen.withValues(alpha: 0.3);

  // Overlay Colors
  static const Color overlayLight = Color(0x0A000000);
  static const Color overlayMedium = Color(0x1A000000);
  static const Color overlayDark = Color(0x33000000);
  static const Color overlayDarker = Color(0x66000000);

  // Chart Colors
  static const List<Color> chartColors = [
    primaryPurple,
    primaryBlue,
    primaryGreen,
    primaryOrange,
    primaryRed,
    primaryPink,
    primaryIndigo,
    primaryTeal,
    primaryCyan,
    primaryYellow,
  ];

  // Status Methods
  static Color getStatusColor(String status) {
    switch (status.toLowerCase()) {
      case 'active':
      case 'success':
      case 'completed':
      case 'paid':
        return success;
      case 'pending':
      case 'waiting':
      case 'processing':
        return warning;
      case 'inactive':
      case 'suspended':
      case 'blocked':
      case 'banned':
      case 'failed':
      case 'error':
        return error;
      case 'verified':
        return info;
      default:
        return textSecondary;
    }
  }

  // Priority Methods
  static Color getPriorityColor(String priority) {
    switch (priority.toLowerCase()) {
      case 'high':
      case 'critical':
      case 'urgent':
        return error;
      case 'medium':
      case 'normal':
        return warning;
      case 'low':
        return success;
      default:
        return textSecondary;
    }
  }

  // Role Methods
  static Color getRoleColor(String role) {
    switch (role.toLowerCase()) {
      case 'admin':
        return badgeOfficial;
      case 'country manager':
      case 'country_manager':
        return badgeOfficial;
      case 'agency':
      case 'agency owner':
      case 'agency_owner':
        return badgeAgency;
      case 'coin seller':
      case 'coin_seller':
        return badgeCoinSeller;
      case 'host':
        return badgeHost;
      case 'vip':
        return badgeVIP;
      case 'moderator':
        return badgeModerator;
      case 'streamer':
        return badgeStreamer;
      default:
        return textSecondary;
    }
  }

  // Badge Methods
  static Color getBadgeColor(String badgeType) {
    switch (badgeType.toLowerCase()) {
      case 'agency':
        return badgeAgency;
      case 'coin_seller':
      case 'coin seller':
        return badgeCoinSeller;
      case 'official':
        return badgeOfficial;
      case 'vip':
        return badgeVIP;
      case 'host':
        return badgeHost;
      case 'moderator':
        return badgeModerator;
      case 'streamer':
        return badgeStreamer;
      default:
        return textSecondary;
    }
  }

  // Tier Methods
  static Color getTierColor(String tier) {
    switch (tier.toLowerCase()) {
      case 'bronze':
        return tierBronze;
      case 'silver':
        return tierSilver;
      case 'gold':
        return tierGold;
      case 'platinum':
        return tierPlatinum;
      case 'diamond':
        return tierDiamond;
      case 'master':
        return tierMaster;
      case 'legend':
        return tierLegend;
      default:
        return textSecondary;
    }
  }

  // Game Methods
  static Color getGameColor(String game) {
    switch (game.toLowerCase()) {
      case 'chess':
        return gameChess;
      case 'carrom':
        return gameCarrom;
      case 'pictionary':
        return gamePictionary;
      case 'trivia':
        return gameTrivia;
      case 'truth or dare':
      case 'truth_or_dare':
        return gameTruthOrDare;
      case 'werewolf':
        return gameWerewolf;
      case 'ludo':
        return gameLudo;
      case 'snake & ladder':
      case 'snake_and_ladder':
        return gameSnakeLadder;
      default:
        return textSecondary;
    }
  }

  // Opacity Methods
  static Color withOpacity(Color color, double opacity) {
    return color.withValues(alpha: opacity);
  }

  // Brightness Methods
  static Color darken(Color color, [double amount = 0.1]) {
    assert(amount >= 0 && amount <= 1);
    final HSLColor hsl = HSLColor.fromColor(color);
    final HSLColor hslDark = hsl.withLightness((hsl.lightness - amount).clamp(0.0, 1.0));
    return hslDark.toColor();
  }

  static Color lighten(Color color, [double amount = 0.1]) {
    assert(amount >= 0 && amount <= 1);
    final HSLColor hsl = HSLColor.fromColor(color);
    final HSLColor hslLight = hsl.withLightness((hsl.lightness + amount).clamp(0.0, 1.0));
    return hslLight.toColor();
  }

  // Contrast Methods
  static bool isLightColor(Color color) {
    final luminance = color.computeLuminance();
    return luminance > 0.5;
  }

  static Color getContrastColor(Color color) {
    return isLightColor(color) ? textInverse : textPrimary;
  }

  // Theme Methods
  static ColorScheme get lightColorScheme => const ColorScheme(
    brightness: Brightness.light,
    primary: primaryPurple,
    onPrimary: textPrimary,
    secondary: accentBlue,
    onSecondary: textPrimary,
    error: error,
    onError: textPrimary,
    surface: surfaceLight,
    onSurface: textPrimary,
  );

  static ColorScheme get darkColorScheme => const ColorScheme(
    brightness: Brightness.dark,
    primary: accentPurple,
    onPrimary: textPrimary,
    secondary: accentBlue,
    onSecondary: textPrimary,
    error: error,
    onError: textPrimary,
    surface: surfaceDark,
    onSurface: textPrimary,
  );
}